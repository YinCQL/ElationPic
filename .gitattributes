# ==========================================================================
# ElationPic — .gitattributes
#
# 目的：统一换行符，避免不同系统上出现整文件 diff。
# 仓库内统一存 LF；Windows 下检出时按文件类型决定是否转 CRLF。
# ==========================================================================

# 默认：自动判断文本/二进制，文本在仓库内统一为 LF
* text=auto eol=lf

# --------------------------------------------------------------------------
# 明确声明为文本（仓库内 LF）
# --------------------------------------------------------------------------
*.php     text eol=lf
*.js      text eol=lf
*.css     text eol=lf
*.md      text eol=lf
*.html    text eol=lf
*.json    text eol=lf
*.ini     text eol=lf
*.conf    text eol=lf
*.sample  text eol=lf

# --------------------------------------------------------------------------
# PowerShell 脚本：Windows 下检出为 CRLF
#
# Windows PowerShell 5.1 对纯 LF 的 .ps1 偶有解析问题，
# 且多数 Windows 编辑器默认 CRLF。这里让它检出为 CRLF 更稳妥。
# --------------------------------------------------------------------------
*.ps1     text eol=crlf
*.bat     text eol=crlf
*.cmd     text eol=crlf

# --------------------------------------------------------------------------
# 明确声明为二进制（不做任何换行转换）
# --------------------------------------------------------------------------
*.jpg     binary
*.jpeg    binary
*.png     binary
*.gif     binary
*.webp    binary
*.ico     binary
*.sqlite  binary
*.zip     binary

# --------------------------------------------------------------------------
# 语言统计：这些是生成物或第三方目录，不计入仓库语言占比
# --------------------------------------------------------------------------
docs/SECURITY-AUDIT.md  linguist-documentation
